#include <pch.h>

#include <algorithm>

#include <Source/imageFilters.h>

namespace Simply2D
{
	namespace imageFilters
	{
        bool blur(SDL_Texture* texture, Span<DrawCall> calls, float factor, float bias) 
		{
			return true;
        }
	}
}